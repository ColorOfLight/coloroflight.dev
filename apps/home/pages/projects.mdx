---
type: page
title: Projects
---

import ProjectCard from '@/components/ProjectCard';
import ProjectList from '@/components/ProjectList';

export const personalProjects = [
  {
    name: "Playground",
    description: "웹 어셈블리 등 다양한 기술을 실험해보기 위한 프로젝트입니다.",
    url: "https://playground.coloroflight.dev",
  },
  {
    name: "Tiny Series",
    description: "컴퓨터 그래픽스의 기반 기술을 GPU를 사용하지 않고 구현한 프로젝트입니다.",
    url: "https://tiny.coloroflight.dev",
  },
];

export const archivedProjects = [
  {
    name: "Explainable WebGPU",
    description: "WebGPU를 이용해 기초 그래픽 개념을 구현하려 했던 프로젝트입니다.",
    url: "https://github.com/ColorOfLight/explainable-webgpu",
  },
];

# Projects

## 개인 프로젝트

<ProjectList>
  {personalProjects.map((project, index) => (
    <ProjectCard key={index} {...project} />
  ))}
</ProjectList>

## 아카이브

<ProjectList>
  {archivedProjects.map((project, index) => (
    <ProjectCard key={index} {...project} />
  ))}
</ProjectList>

---
