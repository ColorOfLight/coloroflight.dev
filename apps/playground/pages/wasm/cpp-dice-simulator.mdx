import Content from './cpp-dice-simulator.content';

# C++ Dice Simulator

This is a simple dice simulator written in C++ that uses WebAssembly to run in the browser. It uses the `rand()` function to generate random numbers.

<Content />